# Server Name and Git sync URL
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${cloud.config-git.repo.uri}
          timeout: 5


#server port no
server:
  port: 8888